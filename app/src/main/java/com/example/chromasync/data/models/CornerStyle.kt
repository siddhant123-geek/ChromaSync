package com.example.chromasync.data.models

enum class CornerStyle(val displayName: String, val radiusValue: Float) {
    MINIMAL("Minimal", 4f),
    STANDARD("Standard", 12f),
    ROUNDED("Rounded", 20f)
}